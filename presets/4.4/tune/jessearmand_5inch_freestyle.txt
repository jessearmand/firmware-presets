#$ TITLE: Freestyle 5-inch 500Hz
#$ FIRMWARE_VERSION: 4.4
#$ CATEGORY: TUNE
#$ STATUS: COMMUNITY
#$ KEYWORDS: 500Hz, Race500, ghost, rc, rx , link, ghost, IRC, GHST, immersionRC, immersion, Freestyle
#$ AUTHOR: Jesse Armand
#$ DESCRIPTION:
#$ DESCRIPTION: This tune can be applied up from 166Hz to 500Hz Race mode on IRC Ghost, but also minimizing delays from RC smoothing.
#$ DESCRIPTION: Filter settings are based on a normal build with RPM filtering
#$ DESCRIPTION: Slightly higher PID values from the defaults
#$ DESCRIPTION:
#$ FORCE_OPTIONS_REVIEW: TRUE

# Set to Defaults

#$ INCLUDE: presets/4.4/tune/defaults_tune_filters.txt

# Defaults end

#$ OPTION BEGIN (UNCHECKED): RC link settings for up to 500Hz

# Set to Defaults

#$ INCLUDE: presets/4.3/rc_link/defaults.txt

# Defaults end

set feedforward_averaging = 2_POINT
set feedforward_smooth_factor = 65
set feedforward_jitter_factor = 7
#$ OPTION END

#$ OPTION BEGIN (UNCHECKED): Filtering for Normal builds with RPM filtering
#$ INCLUDE: presets/4.3/filters/basic_rpm_normal.txt
#$ OPTION END

# RC Smoothing
set rc_smoothing_auto_factor = 20
set rc_smoothing_auto_factor_throttle = 20

# PID profile

#$ OPTION BEGIN (UNCHECKED): VBat sag vbat_sag_compensation at 100%
set vbat_sag_compensation = 100
#$ OPTION END

set pidsum_limit = 1000
set pidsum_limit_yaw = 1000
set d_max_advance = 0

#$ OPTION_GROUP BEGIN: PIDs (select one)
    #$ OPTION BEGIN (UNCHECKED): Overall higher PIDs
    set p_pitch = 54
    set i_pitch = 96
    set d_pitch = 52
    set f_pitch = 143
    set p_roll = 51
    set i_roll = 92
    set d_roll = 46
    set f_roll = 138
    set p_yaw = 51
    set i_yaw = 92
    set f_yaw = 138
    set d_min_roll = 34
    set d_min_pitch = 39
    set simplified_master_multiplier = 115
    #$ OPTION END

    #$ OPTION BEGIN (UNCHECKED): Higher tracking, lower damping
    set p_pitch = 54
    set i_pitch = 96
    set d_pitch = 40
    set p_roll = 51
    set i_roll = 92
    set d_roll = 35
    set p_yaw = 51
    set i_yaw = 92
    set simplified_pi_gain = 115
    set simplified_dmax_gain = 50
    #$ OPTION END
#$ OPTION_GROUP END

#$ OPTION BEGIN (UNCHECKED): Dynamic Idle at minimum 2500 rpm
set dyn_idle_min_rpm = 25
#$ OPTION END

#$ OPTION BEGIN (UNCHECKED): Lower Dynamic Idle P gain (high power-to-weight ratio)
set dyn_idle_p_gain = 40
#$ OPTION END
